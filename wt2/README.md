# Wt Veelhoek voorbeeld

## Start zonder Docker

Dit zijn de stappen:

~~~~
cd veelhoek
mkdir build
cd build
cmake ..
make
cd ..
./run.sh
~~~~

Met de `run.sh` wordt poort 8080 geopend.

## Start met Docker

Zie `Makfile`.
